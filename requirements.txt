# CLAM-TR — software dependency manifest
# Source of truth: study Methodology "Compute Environment" + the recorded Google
# Colab Pro+ runtime (2026). Hardware: NVIDIA Tesla T4 (15.6 GB) for the
# 9,128-slide factorial (both encoders); NVIDIA A100 80 GB for the exploratory
# LoRA branch only. Python 3.10/3.11 (Colab Pro+ 2026).
#
# These are the libraries the pipeline imports, with the lower-bound versions
# documented in the project records. For a byte-exact lock, capture
#     pip freeze > requirements-lock.txt
# from the Colab runtime that produced the headline results and commit it next
# to this file. (System dependency for OpenSlide: apt-get install openslide-tools libgl1.)
#
torch>=2.0                  # PyTorch 2.x; mixed precision via torch.amp; recorded snapshot used 2.9.0+cu126
torchvision>=0.15
timm>=0.9                   # ViT-L/16 (UNI) and ViT-H/14 (UNI2-h) backbones
huggingface_hub>=0.20       # gated MahmoodLab/UNI + MahmoodLab/UNI2-h model & feature releases
openslide-python>=1.3       # WSI .tiff reading (needs system openslide-tools / libopenslide)
h5py>=3.9                   # UNI2-h .h5 feature caches
opencv-python-headless>=4.8
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3           # QWK (cohen_kappa_score, weights='quadratic'); LogisticRegression linear probe
scipy>=1.11                 # paired t-test, Wilcoxon signed-rank, percentile bootstrap
statsmodels>=0.14           # Benjamini-Hochberg FDR (statsmodels.stats.multitest.fdrcorrection)
matplotlib>=3.7
seaborn>=0.12
